using System.Numerics;
using Raylib_cs;

public class Note: Sprite
{
    public Note(Vector2 posistion,  Texture2D texture)
    {
        _posistion = posistion;
        _texture = texture;
    }

    public void Update()
    {
        return;
    }
}